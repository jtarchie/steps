# provider: custom is a scheme the pipeline describes; with no signature: there
# is nothing to check a delivery against.
# expect: provider: custom needs source.signature

resources:
- name: hook
  type: webhook
  source:
    provider: custom
    secret_env: HOOK_SECRET

jobs:
- name: build
  plan:
  - get: hook
    trigger: true
