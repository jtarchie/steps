# prompt: is the task given to an agent's model. A task step holds no
# conversation, so a prompt here did nothing — rejected rather than read as
# if it were in force.
# expect: prompt is only valid on agent steps

jobs:
- name: build
  plan:
  - task: compile
    run: "true"
    prompt: Please build carefully.
