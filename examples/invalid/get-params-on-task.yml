# get_params configures the implicit get a PUT runs. A task has no such fetch,
# and a get step spells its own fetch options params:.
# expect: get_params is only valid on put steps

jobs:
- name: build
  plan:
  - task: work
    run: echo working
    get_params: { depth: 1 }
