# context: grants the set_context tool, and a shell command has no tools to
# grant. A task records facts by writing files, not by calling a model tool.
# expect: context is only valid on agent steps

jobs:
- name: build
  plan:
  - task: work
    inputs: []
    run: "true"
    context: write
