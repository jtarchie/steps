# Build Tasks: Run page — the transcript above the fold

Generated from: .design/run-page-ia/DESIGN_BRIEF.md and INFORMATION_ARCHITECTURE.md
Date: 2026-09-25

Repo rules that shape every task (CLAUDE.md): TDD outside-in — the e2e assertion in `./e2e` first where one exists, then `internal/web` package tests; every change proven by sabotage; `liveregion_test.go` decides whether a new stateful line needs a region; `TestStreamAppliedToThePageIsTheReloadedPage` and `TestStreamDrawsTheSameMarkupThePageDoes` must stay green for anything that changes a step row; docs/web.md changes in the same commit as the prose it describes. Aesthetic: terminal-native, inherited from nav-overflow — reuse `.metaline`, `.diffnote`, `.errblock`, `.spend`, `.keyhint`; no new color, no new font size, no new route.

## Foundation
- [ ] **The failing step is the error's home** (risk first: it crosses runview → web → stream → e2e): invert `DistinctError`. A failed step's body always shows its own error; the head's `.errblock` renders only when the run has a job error that no failed step's error is contained in (`runView.HeadError()`: `JobError` unless `InnermostFailure` exists and its error is a substring of it — check every failed step, not just the innermost, since a `try:` can fail a child the job did not die of). The MCP `.errhint` follows the error: under `.errblock` when that renders, under the innermost failing step's error when the step holds it (`MCPBlamed` stays on the run). Done when `e2e/e2e_web_test.go`'s failed-run case asserts the error text appears inside `class="step failed` … `_body` and NOT in an `.errblock`, a package test proves a placement/pull failure with no steps still renders `.errblock`, and `TestARunThatFailedOnAnMCPServerLinksTheMCPTab` finds the hint in the step. `TestStreamAppliedToThePageIsTheReloadedPage`'s fixture note ("a job error that quotes a step's own … changes what that step's row shows without an event on the step") stops being true — the row no longer changes on `job.finished` — verify the test still exercises something (it compares to the reload; it will pass) and update its comment. _Modifies: `internal/runview/runview.go` (`DistinctError` → `Error` shown unconditionally, or rename), `internal/web/model.go` (`HeadError`), `run.html` step body + head, docs/web.md "A failed run leads with the error" bullet._ Sabotage: put the old `strings.Contains` back and watch the e2e fail.

## Core UI
- [ ] **Two head lines, and the sections they point at, below the transcript**: split the metaline into an outcome `<p class="metaline">` (status · `failed at` · duration · started) and a provenance `<p class="metaline faint">` (config · workspace · after · feeds · `spend <thousands> tokens · <cached>% · <cost|unpriced>[ · N truncated][ so far] <a href="#spend">→ #spend</a>` · `<N> placed <a href="#machines">→ #machines</a>`), separators only between present entries. Move the `.spend` and `.placement` sections after the `.callout`, as `<section id="spend" aria-labelledby="spend-lbl">` / `<section id="machines" aria-labelledby="machines-lbl">` with the existing `.lbl` spans carrying those ids, and `section.spend { scroll-margin-top: 3.2rem }` plus a `:target` treatment matching `.step:target`. Test the seam: a package test renders a run with usage and a placement and asserts every `href="#…"` in the provenance line names an `id` present in the page, that `#spend` appears AFTER `id="transcript"` in the body, and that a run with no usage has neither the entry nor the section. Extend `TestFailedRunNamesItsInnermostFailure` to assert the lede is on the FIRST metaline and `config` on the second. Done when a pr-review-shaped fixture (10 agent steps) renders `id="transcript"` within the first ~40 lines of body. _Modifies: `run.html`, `app.css` (`.metaline.faint`, section anchor rules), `model.go` if the spend line needs a `SoFar`/`Placed` helper._ _Depends on: error home (the outcome line's shape)._
- [ ] **One Δ line**: replace the two `.diffnote`s (config-changed, changed-content) with one composed from `ComparedTo`, `ComparedConfig`, `Changed`: `Δ vs last passed [sha]: config changed [sha] · build, test changed content` / `… config changed [sha] · no step's content moved` / `… : build changed content — everything else replayed from cache`. `replayed from` keeps its own line. Done when a package test renders each of the three combinations and asserts one `.diffnote` (plus the replay line when set) with the expected phrase, and none when there is no prior success. _Modifies: `run.html`; a `DriftLine`-shaped helper on `runView` only if the template branching gets unreadable._ Sabotage: drop `ComparedConfig` from the composition; the config-changed case must fail.
- [ ] **Transcript head carries the key hint**: wrap `.keyhint` in `<div class="transcripthead">` (flex, `justify-content: flex-end`, margin-bottom 6px) directly above `#transcript`; remove its standalone row. Still `aria-hidden` and hidden under `hover: none`. Done when the DOM order test above (transcript within the first screen) counts fewer lines, and `TestVendoredHtmxStillSupportsWhatTheStreamSends`/stream tests are untouched (nothing inside `#transcript` moves). _Modifies: `run.html`, `app.css`._

## Interactions & States
- [ ] **Live and edge states**: `so far` suffix on the spend entry only while `Run.Running`; the provenance entries render on a running run when their sections do (`HasSpend`/`HasPlacements` already gate). No-step failure: `.errblock` above the strip, no `failed at`, transcript placeholder says `This run recorded no steps.`. Errored (infra) runs: same as no-step. Run the `liveregion_test.go` probe after the head change: the metalines were outside every region before and their content changes only on the done reload; if the probe now flags the spend entry, the answer is that it changes only when the tables do (same as today) — record why in the test's allow-list rather than adding a region. Covers: running, failed-with-step, failed-no-step, errored, passed-with-spend, passed-without-spend.

## Responsive & Polish
- [ ] **Wrapping at 375 and 1280**: render a failed pr-review-shaped run with the cloakbrowser script (no Chrome MCP) at both widths; both metalines wrap without horizontal scroll, the transcript's first row is on the first screen at 1280, the sections below keep `.spendscroll`. Breakpoints: the existing `max-width: 640px` block only.
- [ ] **Accessibility pass**: `aria-labelledby` on both sections resolves; `failed at` link is the first focusable after `.actions`; measure `--faint` on `--bg` at 12px for AA — if it fails, the provenance line's text uses `--dim` and only `.sep` stays `--faint`; `:target` on a section is visible without color alone (border, as `.step:target`). Add the run page's new anchors to `crosslinks_test.go` if it has an in-page-anchor check; else assert them in the seam test above.
- [ ] **Docs**: docs/web.md "The transcript" section — the failed-run bullet says the error is on the step; the spend and machines bullets say the panels sit under the transcript and are linked from the head; one sentence on the two head lines. `TestDocsPagesListed` and the fenced examples are untouched (no yaml changes).

## Review
- [ ] **Design review**: Run `/design-review` against the brief with screenshots of a failed run with ten agent steps at 1280 and 375, a running run, and a no-step failure.
