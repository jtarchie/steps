# Build Tasks: Navigation overflow — fewer hops to the run that matters

Generated from: .design/nav-overflow/DESIGN_BRIEF.md and INFORMATION_ARCHITECTURE.md
Date: 2026-09-24

Repo rules that shape every task (CLAUDE.md): TDD outside-in — the e2e test in `./e2e` first, then `internal/web` package tests; every fix/feature proven by sabotage; `liveregion_test.go` fails any new stateful region that htmx does not name; `TestBreadcrumbsOnDetailPages`, `TestDistinctHeadings`, `crosslinks_test.go` and `statusvocab_test.go` are the contracts new links and status words must satisfy; docs/web.md's route table changes in the same commit as the route. Aesthetic: terminal-native, reuse `.st-*`, `.metaline`, `.crumbs`, `.tablewrap`; no new color, no new font size.

## Foundation
- [x] **Job detail route + job redirect** (e9c0dd2): Register `GET /p/:pipeline/jobs/:job/detail` serving today's `handleJob` (title `$ steps job <job>`, crumb `jobs / <job> / detail`), and make `GET /p/:pipeline/jobs/:job` a 302: latest run → `/runs/:id`; no run but a pending queue row for this job → `/jobs/:job/follow?since=<enqueued_at ms>`; neither → `/detail`. Done when an e2e test runs a job through `cli.Run`, GETs `/jobs/build` and lands on the transcript, and a second e2e case with a queued row lands on follow with the right `since`. _Modifies: `web.go` routes, `handlers.go` (`handleJob` → `handleJobDetail`, new `handleJobRedirect`), `handleTrigger`/`handleFollow` crumbs, `handleRun` crumb job link → `/detail`, `handleNode` crumb. Moves ~35 test references across nine `internal/web/*_test.go` files from `/jobs/<job>` to `/jobs/<job>/detail` (liveregion's "job page" entry included). Updates docs/web.md's route table (`…/jobs/:job` row becomes the redirect rules; new `…/jobs/:job/detail` row)._ Risk-first: this is the one change every other link depends on.

## Core UI
- [x] **Run strip on the run page** (3effa6f): Under the run head, a `<nav class="runstrip" aria-label="Runs of <job>">` of ≤20 chips newest-first (`ListRuns(job, 20)`), each an `<a class="st st-<word>" href="/runs/:id" title="<short id> · <duration>">` labelled with the relative time; the current run's chip carries `aria-current="page"` and shows its short id visibly; a pending queue row for the job renders first as `<a class="st st-pending" href="…/follow?since=…">queued</a>`; trailing `<a href="…/detail">all runs</a>`. The strip is a polled region: `id="run-strip" hx-get="{{.Nav.URL}}" hx-trigger="every[!document.hidden] 2500ms" hx-select="#run-strip" hx-swap="outerMorph" hx-select-oob="#attention:outerMorph,#statusline:outerMorph"`. Done when a package test renders a run page, starts a sibling run, re-renders, and the new chip appears inside `#run-strip` (add the entry to `liveregion_test.go`), and a test asserts the queued chip's `since` equals the queue row's enqueue time. _New component `.runstrip` in app.css (flex-wrap, gap 6px, chips as inline anchors, current chip underlined/`--fg`); reuses `.st-*`, `.hash`._ _Depends on: job detail route (for the trailing link)._
- [ ] **Failure lede + deps line in the run head**: In `run.html`'s metaline, after the status: `<span class="sep">·</span>failed at <a href="#<anchor>"><step name></a>` when the run failed and the fold has an innermost failed step (the same resolution `f` uses in `layout.html`; expose it from the view as `view.FailedAt` so the template and the key agree), and `after <a>build</a> [source] · feeds <a>deploy</a> [image]` from the job's upstream/downstream when either exists (links → `/jobs/:job`, i.e. their latest run). Done when a package test renders a failed run with a nested failing step and finds the link to the innermost row's anchor, and a test with an unnested pass shows no lede. _Modifies: `run.html`, `handlers.go` `handleRun` (upstream/downstream already computed for the job page — reuse), `model.go`/runview for `FailedAt`. Reuses `.metaline`, `.sep`, `.res`._
- [ ] **Job chips on the overview**: Replace the `Jobs` count cell in `overview.html` with the pipeline's jobs in config order as `<a class="st st-<word>" href="/p/<slug>/jobs/<job>"><job></a>` (`st-none`-style faint for never-ran, reusing the DAG's `none` treatment), wrapping inside the cell; the `jobs` count moves into the cell's `title`. Done when an e2e test serves two pipelines, runs one job to failure, GETs `/`, finds the red chip, follows its href and lands on the failing transcript (the cold-tab flow, end to end). `overviewPipelines` gains per-job latest status from `LatestRunByJob`, which the shell already calls per pipeline — pass it through rather than querying twice. _Modifies: `overview.go`, `overview.html`; `overview_test.go` `TestRootListsEveryServedPipeline` extended. New `.jobchips` rule (flex-wrap, gap 4px 10px)._ _Depends on: job redirect (the chip href)._

## Interactions & States
- [ ] **Strip states**: current (marked, id visible), running (`◐`, yellow, pulses only via the existing `.st-running` glyph — no animation), queued (`○`, → follow), failed/errored/aborted (`✗`), skipped/never (faint). Empty strip (job has exactly this run and nothing queued) renders the nav with one chip, not nothing. Verify the `queued` chip disappears and a `running` chip takes its place within one poll by re-rendering after `startRunningBuild` in the package test. _Covers: current, running, queued, passed, failed, single-run._
- [ ] **Redirect edge states**: `/jobs/:job` for an unknown job → 404 (as `handleFollow` does); a run reaped by retention between the list and the redirect → the target 404 page already says so, no special case; read-only server → redirect identical (no mutation). Package tests for the 404 and for read-only. _Modifies: `handlers.go`; extends `TestUnknownPipelineAndRun404`._

## Responsive & Polish
- [ ] **Wrapping and touch**: Strip and chips wrap below 640px with no horizontal scroll; the current chip shows its id visibly (hover titles are unreachable on touch); chip tap targets ≥ 24px tall via line-height. Breakpoints: the existing `max-width: 640px` block. Verify by rendering at phone width with the cloakbrowser script the repo's memory prescribes (no Chrome MCP).
- [ ] **Accessibility pass**: `aria-current="page"` on the current chip; `<nav aria-label>` on the strip; the lede link precedes the transcript in tab order; chips keep stable `id`s (`strip-<short id>`) so `outerMorph` preserves focus across polls (add the strip to `TestLiveRegionsAreDrivenByHtmx`'s focus check if it has one, else assert ids are present); status carried by the `.st` glyph, never color alone (`statusvocab_test.go` already enforces the vocabulary — add the strip and chips to its page list).
- [ ] **Docs and keyboard hint**: docs/web.md: route table rows for `…/jobs/:job` (redirect rules) and `…/jobs/:job/detail`; "What it shows" gains one paragraph on the strip and the lede; the transcript's Keyboard bullet notes the lede is the `f` key's link. `TestDocsPagesListed`/route mentions stay green.

## Review
- [ ] **Design review**: Run `/design-review` against the brief with screenshots of `/` (two pipelines, one red), a failed run with the strip, and the detail page.
