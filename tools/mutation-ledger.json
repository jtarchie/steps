{
  "packages": {
    "./internal/merkle": {
      "swept_at": "fc9bbc9",
      "date": "2026-09-19",
      "killed": 161,
      "lived": 5,
      "timed_out": 0,
      "not_covered": 30,
      "efficacy": 96.99,
      "note": "Was 78.31 with 36 survivors. 31 were one class - a field folded into the key only when set, with nothing proving that setting it moves the key or that leaving it unset does not - and keyfields_test.go now kills them. The 5 left are plan-time control flow on three lines (planNonGetNode's across: dispatch, planTaskNode's dynamic flag, acrossNode's error path): NOT triaged. They are the kind ./e2e usually kills and package mode never runs; nobody has applied them and looked.",
      "equivalent": []
    },
    "./internal/outcome": {
      "swept_at": "fc9bbc9",
      "date": "2026-09-19",
      "killed": 4,
      "lived": 0,
      "timed_out": 0,
      "not_covered": 3,
      "efficacy": 100,
      "note": "",
      "equivalent": []
    },
    "./internal/agent": {
      "swept_at": "b7c68ec",
      "date": "2026-09-21",
      "killed": 140,
      "lived": 46,
      "timed_out": 0,
      "not_covered": 35,
      "efficacy": 75.27,
      "note": "PARTIAL: only the ten files steps#137 added were swept (ere, tree, treecontainer, treehost, treepath, treesearch, treesetup, probe, fatal, runnerfactory). The other 41 files in the package are unmeasured, so this row is not a statement about internal/agent as a whole. An unscoped first attempt reached editfile.go and conversation.go before it was stopped and showed ~35 survivors in editfile.go's edit-matching heuristics alone - unrelated to this change, and the obvious next group to sweep. Was 73.12 with 50 survivors. Four were real gaps and are now tested: the lexical half of the container path confinement (every case in the confinement test resolved to a real path, so rejectLinkEscape answered them all and inverting the string check left the test green - a path that escapes lexically with a nonexistent target was the missing case, and it is the security-relevant one); caseFolds indexing out[1] on a one-rune fold set, which only happens for a case-insensitive pattern holding a caseless character like (?i)v2; a NUL at position 0 slipping the container write guard; and a blind spot in the Go-based differential itself, which claimed ERE was a subset of RE2 - it is not, because RE2 reads a backslash inside a bracket as an escape where POSIX reads it as a literal, so the correct rendering of [a\\\\b] is [a-b\\] which both real greps accept and Go rejects. That case moved to the docker parity test. WORKERS=1 throughout: this package now spawns containers in its own tests.",
      "equivalent": [
        {
          "mutant": "ere.go | CONDITIONALS_BOUNDARY | if len(out) > 1 && out[1] < out[0] {",
          "why": "the < to <= form is equivalent: out[1] is a case fold of out[0] and a rune is never equal to its own distinct fold, so the branch never sees equality"
        },
        {
          "mutant": "ere.go | CONDITIONALS_BOUNDARY | coversHigh := pairs[len(pairs)-2] <= asciiMax+1 && pairs[len(pairs)-1] == unicodeMax",
          "why": "differs only for a class whose last low range ends exactly at 0x80, which RE2 produces only for a pattern written at that exact code point; reachable in principle, not by anything a model writes, and rendering stays correct either way for every class in the corpus"
        },
        {
          "mutant": "ere.go | CONDITIONALS_BOUNDARY | if pairs[i+1] > asciiMax {",
          "why": "same edge as coversHigh, one code point down: differs only for a range ending exactly at 0x7F"
        },
        {
          "mutant": "ere.go | CONDITIONALS_BOUNDARY | for lo <= hi && strings.ContainsRune(classSpecials, lo) {",
          "why": "the <= to < form changes nothing observable for a single-rune range: an unlifted special is still written as a one-rune range and renders to the same bracket body, checked against both greps by the bracket-specials parity case"
        },
        {
          "mutant": "ere.go | CONDITIONALS_BOUNDARY | if lo > '\\n' || hi < '\\n' {",
          "why": "splitAroundNewline's fast path; the boundary forms still split the newline out of every range that contains one, which is the only property that matters - a newline in the rendered pattern is what grep reads as a second pattern"
        },
        {
          "mutant": "ere.go | CONDITIONALS_BOUNDARY | if r <= asciiMax && strings.ContainsRune(ereSpecial, r) {",
          "why": "differs only for U+007F, which is not in ereSpecial, so the second half of the conjunction decides either way"
        }
      ]
    },
    "./internal/pipeline": {
      "swept_at": "6645ad0",
      "date": "2026-09-22",
      "killed": 125,
      "lived": 35,
      "timed_out": 0,
      "not_covered": 32,
      "efficacy": 78.13,
      "note": "First sweep, scoped SINCE=9ea6941 (get.go, task.go, put.go, venue.go, resourceplace.go, holders.go). PARTIAL TRIAGE: the 10 survivors in holders.go (the file steps#138 added) were worked through \u2014 2 real gaps now pinned by holders_test.go, 4 killed by ./e2e when applied by hand (the placedTaskSpace/placedPutSpace tag-and-store guards, the empty-held guard, withRemoteInputs' empty guard), 4 equivalent. The other 25 are PRE-EXISTING lines in files this work happened to touch (get.go 11, task.go 9, venue.go 2, put.go 1, resourceplace.go 1) and are NOT triaged; they belong to a pipeline sweep of its own. Efficacy below counts them all.",
      "equivalent": [
        {
          "mutant": "holders.go | CONDITIONALS_NEGATION | if err != nil { (after HoldRemote)",
          "why": "HoldRemote fails only on an artifact name the plan already validated or a digest the venue always sets; no pipeline can reach it"
        },
        {
          "mutant": "holders.go | CONDITIONALS_NEGATION | if sink == nil { (heldFrom)",
          "why": "a defensive guard: every caller runs under the sink runPlacedStage installs"
        },
        {
          "mutant": "holders.go | CONDITIONALS_NEGATION | if sink == nil { (noteHeld)",
          "why": "same guard, other side"
        },
        {
          "mutant": "venue.go | CONDITIONALS_NEGATION | if err != nil { (RecordPlacement)",
          "why": "only a log line reads the error; a bookkeeping write must never fail a step that did its work"
        }
      ]
    },
    "./internal/shim": {
      "swept_at": "6645ad0",
      "date": "2026-09-22",
      "killed": 84,
      "lived": 0,
      "timed_out": 23,
      "not_covered": 9,
      "efficacy": 100,
      "note": "First sweep, scoped SINCE=9ea6941 (shim.go, dataplane.go, produced.go). No survivors. 23 TIMED OUT, every one an err-check negation in the frame loop or the store PUT: the peer harness then waits for a frame that never comes, which is a real kill; spot-checked by hand after the sweep, see the report.",
      "equivalent": []
    },
    "./internal/store/sqlite": {
      "swept_at": "6645ad0",
      "date": "2026-09-22",
      "killed": 7,
      "lived": 0,
      "timed_out": 1,
      "not_covered": 0,
      "efficacy": 100,
      "note": "First sweep, scoped SINCE=9ea6941 (placement.go only). The one TIMED OUT (RunPlacements' err check negated) was applied by hand: it hangs the conformance suite, a real kill.",
      "equivalent": []
    },
    "./internal/venue": {
      "swept_at": "6645ad0",
      "date": "2026-09-22",
      "killed": 60,
      "lived": 6,
      "timed_out": 5,
      "not_covered": 3,
      "efficacy": 90.91,
      "note": "PARTIAL: scoped SINCE=9ea6941 and stopped after 74 mutants (dataplane.go, exec.go, placement.go, pull.go, and session.go up to short()); transfer.go, venue.go, worker.go and the rest of session.go are unswept. Serialized (WORKERS=1) at ~50s a mutant, and every negated handshake err-check hangs the peer for the full 12-minute timeout, so the rest is a day of machine time: do it file-group by file-group. Of the 6 survivors, 2 were real and are pinned (TestPushWaitsForTheStoreToHaveIt, TestStoreUploadsLeaveNoPackedCopyBehind); 4 equivalent. The 5 TIMED OUT are handshake and command-loop err-negations: a hung peer, a real kill.",
      "equivalent": [
        {
          "mutant": "dataplane.go | ARITHMETIC_BASE | artifacts := make([]wire.UploadArtifact, 0, len(names)+len(s.remoteInputs))",
          "why": "a capacity hint"
        },
        {
          "mutant": "dataplane.go | CONDITIONALS_NEGATION | if err != nil { (after swapFetched in fetchViaStore)",
          "why": "swapFetched fails only on a local remove or rename nothing can induce without pulling the tree out from under the session; the staging MkdirTemp fails first"
        },
        {
          "mutant": "session.go | CONDITIONALS_BOUNDARY | if len(build) > 12 {",
          "why": "short() only trims a hash in an error message"
        },
        {
          "mutant": "session.go | CONDITIONALS_NEGATION | if len(build) > 12 {",
          "why": "short() only trims a hash in an error message"
        }
      ]
    },
    "./internal/wire": {
      "swept_at": "6645ad0",
      "date": "2026-09-22",
      "killed": 65,
      "lived": 7,
      "timed_out": 0,
      "not_covered": 11,
      "efficacy": 90.28,
      "note": "First sweep, scoped SINCE=9ea6941 (frame.go, tar.go, message.go). 7 survivors: 5 boundary checks now pinned by TestFrameLimitsAcceptTheValueItself and TestFrameLimitsRefuseOnePast, the withinRoot climb by TestPackPathsRefusesASymlinkedName's esc/never case; 1 equivalent.",
      "equivalent": [
        {
          "mutant": "tar.go | CONDITIONALS_NEGATION | if err == nil && closeErr != nil {",
          "why": "Close of a file opened for reading does not fail on any filesystem a test can arrange; the branch guards a kernel error nobody can induce"
        }
      ]
    },
    "./internal/workspace": {
      "swept_at": "6645ad0",
      "date": "2026-09-22",
      "killed": 129,
      "lived": 9,
      "timed_out": 0,
      "not_covered": 25,
      "efficacy": 93.48,
      "note": "First sweep, scoped SINCE=9ea6941 (workspace.go, stepcache.go, remote.go, placed.go, cache.go). 9 survivors: validateCachedNames' mapped-path check and discardStaged's committed skip now pinned by stepcache_sweep_test.go; 7 equivalent.",
      "equivalent": [
        {
          "mutant": "stepcache.go | CONDITIONALS_NEGATION | if err != nil { (StoreStep best-effort, SaveStepCache)",
          "why": "only a log line reads the error"
        },
        {
          "mutant": "stepcache.go | CONDITIONALS_NEGATION | if err != nil { (discardStaged remove)",
          "why": "only a log line reads the error"
        },
        {
          "mutant": "workspace.go | CONDITIONALS_NEGATION | if err != nil { (CloseBuild)",
          "why": "only a log line reads the error"
        },
        {
          "mutant": "workspace.go | CONDITIONALS_NEGATION | if err != nil { (CloseSpace)",
          "why": "only a log line reads the error"
        },
        {
          "mutant": "workspace.go | CONDITIONALS_NEGATION | if removeErr != nil { (sweep)",
          "why": "only a log line reads the error"
        },
        {
          "mutant": "workspace.go | CONDITIONALS_NEGATION | if removeErr != nil { (newSpaceLeaving cleanup)",
          "why": "only a log line reads the error"
        },
        {
          "mutant": "workspace.go | INCREMENT_DECREMENT | b.generations[name]++",
          "why": "rememberDigest compares generations for equality, so any change of the counter invalidates a walk in flight"
        }
      ]
    }
  }
}
