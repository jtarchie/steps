PASS

## Acceptance criteria walk

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | Steps inside `in_parallel:` run concurrently; `limit:` bounds how many at once | PASS | Probe `in-parallel-basic.yml`: two tasks run concurrently (`[branch 0]` and `[branch 1]` start together, both produce output). Go test `TestRunInParallelLimit` (`internal/pipeline/in_parallel_test.go`) asserts serialization with `limit: 1`. |
| 2 | `fail_fast: true` cancels siblings on the first failure, with no orphaned model calls | PASS | Go test `TestRunInParallelFailFast` (`internal/pipeline/in_parallel_test.go`) completes well before the 10s sleep child, proving cancellation. E2E test `TestEndToEndInParallelFailFastCancelsAgent` (`e2e_test.go:623`) verifies agent cancellation: `fake.requestCount() < 2`. |
| 3 | Duplicate output/artifact names across branches fail at load time | PASS | Go test `TestInParallelDuplicateOutputs` (`internal/config/in_parallel_test.go`) — both flat and nested duplicate cases assert "duplicate" in error. |
| 4 | Concurrent output is labelled per branch | PASS | All probes show `[branch N]` prefix on every child output line (`runInParallelChildren` at `internal/pipeline/pipeline.go:698`). |
| 5 | Nesting works | PASS | Probe `in-parallel-nested.yml`: four nested tasks (`a1`, `a2`, `b1`, `b2`) all run and produce output. Go test `TestRunInParallelNested` also passes. |
| 6 | Docs cover the shared-workspace hazard and point at isolation strategies | PASS | `docs/control-flow.md` has "Workspace safety" section (line ~138) pointing to `workspace.md`. `docs/workspace.md` has a "Concurrency hazard with `in_parallel:`" note (line ~21). |

## Story prose requirements

| Requirement | Verdict | Evidence |
|---|---|---|
| "Naming collisions... must be a **load-time error**" | PASS | `validateInParallelBranchOutputs` (`internal/config/step.go:776-796`), tested in `TestInParallelDuplicateOutputs`. |
| "Silent last-write-wins would be a correctness trap" | PASS | Covered by duplicate-output rejection above. |
| "Workspace safety... must ship with explicit docs" | PASS | Both `docs/control-flow.md` and `docs/workspace.md` updated. |
| "Log interleaving... needs a per-branch label prefix" | PASS | `[branch N]` prefix in `runInParallelChildren` (`pipeline.go:698`) and `runInParallelHookStep` (`hooks.go:196`). |
| "Routing scope: to:/verdicts: targets inside a branch stay within that branch" | PASS | `validateInParallelRouting` (`internal/config/route.go:303-393`) rejects cross-branch routing. Probe confirms: `"routes to 'outside', which is not a step in the same branch"`. |
| "A branch cannot route to a sibling branch or outside the block" | PASS | Same probe as above. |
| "Nesting... recommend allowing from day one" | PASS | `in-parallel-nested.yml` probe passes; `validateInParallelNoGetChildren` and `collectInParallelOutputs` recurse into nested blocks. |
| "Resource contention... `limit:` exists partly *for* this, not just to bound cost" | PASS | Doc mentions this. `limit` field accepted and functional (probe `in-parallel-basic.yml` with `limit: 2`, Go test `TestRunInParallelLimit`). |
| "Fail-fast cancellation — in-flight agent steps must honor context cancellation cleanly, with no orphaned model calls" | PASS | E2E test `TestEndToEndInParallelFailFastCancelsAgent` at `e2e_test.go:623`. |

## Dispatch sites table verification

Walked the plan's table (`repo/experiments/self-build/plans/002.md`) row by row against the diff. All YES rows confirmed present. Spot-checked two Q2 sites:

- `config/route.go:136` (`validateSegment`, `step.Unwrap().Verdicts`): plan-level segment validation does not recurse into in_parallel children, but branch-level routing is validated separately by `validateInParallelRouting`. Verdict routing within in_parallel is a no-op at runtime (all children run concurrently), so missing branch-level verdict validation (`validateBranchRouting` only checks `to:`, not `verdicts:`) has no behavioral impact.
- `pipeline/pipeline.go:695` (`runInParallelChildren`): confirmed passes `nil` for handoff to every child (`runNonGetStep(childCtx, ..., nil, parentHash, nil)` at line 700). Handoff is never delivered inside in_parallel, consistent with the doc.

One site noted as redundant: `validateInParallelRoutingStep` (`internal/config/route.go:325`) iterates `for branchIdx := range step.InParallel.Steps` but validates the same `children` slice against the same `pos` map each iteration. Harmless (same validator called N times with same inputs), but one call would suffice.

## Fixtures vs manifest

All four fixtures listed in `repo/experiments/self-build/specs/002.md` exist and pass `steps test`:
- `examples/in-parallel-basic.yml` ✅
- `examples/in-parallel-fail-fast.yml` ✅
- `examples/in-parallel-nested.yml` ✅
- `examples/in-parallel-routing.yml` ✅

Two additional fixtures shipped:
- `examples/in-parallel-fail-fast-swallow.yml` — regression test: fail_fast: false must NOT swallow
- `examples/in-parallel-routing-noop.yml` — pins that routing is validated but not followed

None carry `# steps-test: skip`.

## Doc accuracy

One minor inaccuracy in `docs/control-flow.md` Routing scope section: "the route target is checked to exist within the same branch" — the `checkInParallelHandoff` function (`internal/config/handoff.go:239-265`) actually checks handoff routes against the plan-level segment's `targeted` map, not against branch-level positions. This produces the same practical result (handoff on children inside in_parallel is either rejected for requiring a route target, or passes for note-only but is never delivered), so there is no behavioral defect, just a slight doc imprecision.

## Overall

All acceptance criteria satisfied. All six fixtures pass. The dispatch sites table is covered. The implementation is correct and complete.
