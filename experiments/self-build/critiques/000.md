PASS. The feature is fully and correctly implemented: all four step types support `attempts:` and `timeout:` with proper retry/timeout semantics, hooks fire once per step (not per attempt), assert evaluates inline per attempt (reasonable behavior matching the test cases), timeouts classify as errored, neither field is hashed, visibility via attempt markers is present, and both examples and comprehensive docs have been added. Minor issue: a comment incorrectly states timeout is "invalid on get steps" when it's actually implemented for get steps—this is just documentation, not a code defect.