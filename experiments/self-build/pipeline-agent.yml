# Walks the roadmap one story at a time -- plan, implement, gate, review, PR --
# using steps' own `agent:` step type rather than shelling out to a coding CLI,
# which is what lets the whole experiment run against a local or cheap model.
# An earlier variant (pipeline.yml, driving `claude -p` from task: steps) has
# been deleted; see README.md.
#
# The repository under development is an ORDINARY INPUT ARTIFACT (`get: repo`),
# not a path the agents reach out to, and that is the whole shape of this
# file. Each agent works in its own workspace where `repo/` is a real git
# clone, so the built-in file tools reach it with no special configuration.
# An earlier revision pointed at a checkout by absolute path, which those
# tools cannot touch (they are confined to the step's workspace), so every
# read/list/write/edit was re-implemented as a custom shell tool that `cd`-ed
# there first -- and `coder` was handed a general-purpose shell just to get
# its narrow tools within reach. Making the repo an input deleted ~190 lines
# of that, and left the narrow tools genuinely narrow.
#
# `put: repo` commits, pushes to a per-story branch, and opens a PR via
# `gh pr create`. A failed review never reaches it, so the real checkout
# is never left dirty by an abandoned attempt.
#
# gopls is granted ONE tool (plus go_diagnostics for coder): finding every
# call site of a symbol without false-positiving on a comment or missing a
# differently-spelled one is the thing a text search genuinely cannot do.
# Finding WHERE something lives is search_files' job. MCP tools are exposed
# with the server name prefixed ("gopls__go_symbol_references"). Needs `gopls`
# on PATH.
#
# Two model slots, as YAML anchors: `&fast` (planner and the two reviewers,
# which read and judge) and `&coding` (coder, which writes). Both point at
# deepseek-v4-pro today; the split is kept so either persona can move without
# touching the other. Prefer a model quoting a real cached-input price -- the
# prompt caching in internal/agent/openrouter.go is worth nothing against a
# model with no cache discount, which pays full price for the whole re-sent
# history every turn.
#
# The story is turned into FAILING TESTS before any Go is written. `spec`
# runs between the planner and the coder and writes each requirement it can
# express as a self-verifying fixture into examples/, proving each one RED on
# the untouched tree. Nothing new runs them: schema_test.go and
# TestExamplesRun already glob examples/*.yml, so `go test ./...` -- the
# coder's verify_gate and the build-check gate both -- executes them for
# free, and the suite stays red until the feature is real.
#
# This replaced a loop that found defects one per round trip. Measured on
# story 002: three real defects, three full re-reviews, and every instrument
# that found one discarded at the end of the transcript. A fixture is that
# same instrument, kept -- so the coder's target is runnable rather than
# prose, defects surface in a two-second gate rather than a five-minute
# conversation, and a defect found once cannot silently return.
#
# The implementation is THREE steps for the other half of that measurement:
# the same run hit `agent.compaction_stalled` with 42 dispatch sites across
# 19 files, which is not a prompt problem but a context-size one. So the work
# splits along this repo's own acyclic package graph -- coder_config (types,
# validation, schema; gated on BUILD only, since nothing there can turn a
# fixture green alone), coder (the runtime that does turn them green, full
# gate), coder_docs (prose last, once the behavior is real and proven). Three
# fresh conversations instead of one that overflows, each handing the next a
# runner-computed "Files touched" note. Docs go LAST deliberately: every
# doc-contradicts-code defect this experiment has produced came from writing
# the prose before the behavior existed.
#
# The review gate is TWO agents, not one, and they must both pass. `reviewer`
# reads the diff and asks whether what was built is correct; `completeness`
# is denied the diff and asks what the story required that nothing
# implements. See the comment above the completeness agent for why a second
# lens is not the same thing as a second sample. Both now write what they
# find as a FIXTURE rather than only as prose, which is what makes a finding
# outlive its own review.
#
# Both resources are GitHub-backed and need `gh` authenticated (GH_TOKEN or
# gh auth login): `repo` clones and opens the PR, and `stories` reads the
# roadmap from open issues labeled `story` rather than from files in the repo.
# That last part is what lets the pipeline retire its own work -- the PR body
# gets a "Closes #N", so a merged story stops appearing in the next check:,
# with nothing to edit here.
resource_types:
- name: stories
  config:
    # One version per OPEN issue labeled `story`. The stories used to be
    # markdown files in the repo; an issue carries the same text plus three
    # things a file could not: state (closing an issue retires the story, with
    # no edit here), a label to select on, and a number a PR can reference so
    # merging closes it.
    #
    # `num` is the 3-digit story number parsed out of the title, NOT the issue
    # number: it names the branch and the plans/<num>.md and prs/<num>/ paths,
    # which already exist for earlier stories. An issue whose title doesn't
    # match the "Story NNN:" shape is skipped rather than guessed at.
    #
    # sort_by(.num) then .[0:limit] is the story SELECTOR. Every open story is
    # a candidate, so limit: 1 (the default in resources: below) means "work
    # the lowest-numbered open story", which is what the old glob did by being
    # hand-narrowed to 001-*.md. Raise it to walk several per run -- but note
    # `version: every` runs them one after another, and a cold story costs the
    # coder the better part of an hour, so wall-clock is roughly linear in
    # this number, and the only cap is the deliberately-unreachable 2h
    # timeout: each agent step carries.
    check: |
      gh issue list \
        --repo {{ .source.repo | shellquote }} \
        --label {{ .source.label | shellquote }} \
        --state open --limit 100 \
        --json number,title \
        --jq '[ .[]
                | select(.title | test("^Story [0-9]{3}:"))
                | { num: (.title | capture("^Story (?<n>[0-9]{3}):").n),
                    issue: (.number | tostring) } ]
              | sort_by(.num)
              | .[0:{{ .source.limit }}]'
    # The story TEXT now comes from the issue body, so unlike the file era this
    # materializes the actual prose rather than a pointer into repo/.
    in: |
      set -e
      echo {{ .version.num | shellquote }} > ./num
      echo {{ .version.issue | shellquote }} > ./issue
      gh issue view {{ .version.issue | shellquote }} \
        --repo {{ .source.repo | shellquote }} --json title --jq .title > ./title
      gh issue view {{ .version.issue | shellquote }} \
        --repo {{ .source.repo | shellquote }} --json body --jq .body > ./body.md

- name: repo
  config:
    check: |
      gh api repos/jtarchie/steps/commits?per_page=1 --jq '[{ref: .[0].sha}]'
    # A clone, not a copy: agents get a real working tree, so go test,
    # golangci-lint and git diff all behave normally, and the origin is
    # untouched until the put.
    #
    # GIT_CONFIG_GLOBAL=/dev/null is load-bearing, not hygiene. An operator's
    # ~/.gitconfig may carry `url.ssh://git@github.com/.insteadOf
    # https://github.com/`, which silently rewrites this HTTPS clone to SSH
    # and makes it depend on a loaded ssh-agent -- so it passes on the machine
    # that set it up and fails with "Permission denied (publickey)" the moment
    # the agent stops answering. Dropping the global config strips the rewrite;
    # the credential helper then puts gh's own token back in its place, so
    # auth comes from the same `gh auth` this resource already requires. The
    # commits below pass identity with `git -c`, so nothing else here wanted
    # the global config anyway.
    in: |
      set -e
      export GIT_CONFIG_GLOBAL=/dev/null
      export GIT_CONFIG_COUNT=1
      export GIT_CONFIG_KEY_0="credential.https://github.com.helper"
      export GIT_CONFIG_VALUE_0="!gh auth git-credential"
      git clone --quiet https://github.com/jtarchie/steps .
      git checkout --quiet {{ .version.ref | shellquote }}
    # Runs with the build workspace as cwd, so stories/ and repo/ are both
    # visible. An unchanged tree exits cleanly rather than failing the job.
    out: |
      set -e
      # Same rewrite bypass as in:, for the same reason -- origin is an HTTPS
      # URL, so a global insteadOf would send this push over SSH too.
      export GIT_CONFIG_GLOBAL=/dev/null
      export GIT_CONFIG_COUNT=1
      export GIT_CONFIG_KEY_0="credential.https://github.com.helper"
      export GIT_CONFIG_VALUE_0="!gh auth git-credential"
      num="$(cat stories/num)"
      issue="$(cat stories/issue)"
      # The title already begins "Story NNN: "; the commit subject says the
      # number itself, so strip the prefix rather than printing it twice.
      subject="$(sed -E 's/^Story [0-9]{3}: *//' stories/title)"
      cd {{ .params.path | shellquote }}
      git add -A
      git diff --cached --quiet && exit 0
      git -c user.name=steps -c user.email=steps@localhost \
        commit --quiet -m "self-build: story $num ($subject)"
      branch="self-build/story-$num"
      # refs/heads/ must be spelled out: in: pins the clone with `git checkout
      # <sha>`, so HEAD here is always DETACHED, and git refuses to guess a
      # destination refname from a detached HEAD ("not a full refname").
      git push --quiet origin HEAD:refs/heads/"$branch"
      # Closes #N is appended here, not asked of the reviewer: it is mechanical,
      # and it is what makes merging a self-build PR retire its own story --
      # which in turn is what drops it out of the next check:.
      body="$(mktemp)"
      cat "experiments/self-build/prs/$num/body.md" > "$body"
      printf '\nCloses #%s\n' "$issue" >> "$body"
      gh pr create \
        --base main \
        --head "$branch" \
        --title "$(cat experiments/self-build/prs/$num/title)" \
        --body-file "$body"

# gopls' MCP server as a local subprocess. cwd: is RELATIVE, so it resolves
# against each agent step's own working directory -- gopls indexes the same
# materialized clone the agents read and edit, rather than some other
# checkout whose paths they could not open.
mcp_servers:
- name: gopls
  command: gopls
  args: [mcp]
  cwd: repo

resources:
- name: stories
  type: stories
  source:
    repo: jtarchie/steps
    label: story
    limit: 1          # lowest-numbered open story only; see the type's check:
- name: repo
  type: repo
  source:
    uri: https://github.com/jtarchie/steps

agents:
# Research only. Holds no shell, so "planner cannot
# modify the repo" is structural rather than a promise: the only write
# it can do is write_file, constrained to its workspace.
- name: planner
  # GLM-4.7-Flash: a 30B-class model tuned for agentic coding and long-horizon
  # task planning, which is exactly what planner and reviewer do. $0.06/M in,
  # $0.40/M out, $0.01/M cached read -- a 6x discount on the re-sent history
  # that dominates a tool-calling conversation. Served by four providers whose
  # context ceilings differ (128k-202k), so the x-session-id pin in
  # internal/agent/openrouter.go is doing real work here.
  source: &fast { model: opencode/deepseek-v4-pro }
  max_turns: 200
  # 80% of deepseek-v4-pro real 1M window. The DEFAULT is 102,400 --
  # 80% of an ASSUMED 128K window (docs/agents-internals.md) -- and
  # leaving it there compacted at a tenth of the actual capacity, which
  # is how a run hit compaction_stalled: after summarizing, the recent
  # window alone still blew the tiny budget, so compaction gave up for
  # good. The 20% headroom is load-bearing, not padding: the estimate
  # counts the conversation only, never the system prompt or the tool
  # schemas resent with every request.
  compact_after_tokens: &ctx_1m 800000
  system_file: "@builtin/planner"
  tools:
  - mcp: gopls
    tools: [go_symbol_references]
    max_output_bytes: 6000
  - read_file
  - list_dir
  - search_files
  - write_file

# Turns the story into FAILING pipeline fixtures before a line of Go is
# written. This exists because of what a run of story 002 measured: the
# reviewer found three real defects, one per round trip, each costing a full
# re-review -- serial discovery, O(n) conversations for n bugs. And every
# instrument that found one was thrown away. `confirm-hook-substep-bug` was a
# four-line pipeline with an assert: that pinned a genuine defect, and it
# existed only inside a review transcript.
#
# A fixture here is the same artifact, kept. It needs NO new machinery,
# which is the whole reason to put it in examples/: schema_test.go globs
# examples/*.yml and validates each against steps.schema.json, and
# TestExamplesRun (assert_test.go) RUNS each one. So a fixture written here
# is automatically executed by `go test ./...`, which is already the coder's
# verify_gate and the build-check gate. Red-until-implemented falls out of
# the existing test suite rather than being enforced by a new step.
#
# The consequences are all the ones the run wanted: the coder's target stops
# being prose and becomes a failing test it can run in two seconds; defects
# are caught by a gate instead of by a five-minute review conversation; and a
# defect found once can never come back, because the thing that found it is
# committed. try_pipeline is granted for one purpose -- proving each fixture
# is RED on the current tree, since a fixture that already passes tests
# nothing at all.
- name: spec
  source: *fast
  max_turns: 200
  compact_after_tokens: *ctx_1m
  system_file: "@builtin/builder"
  tools:
  - read_file
  - list_dir
  - search_files
  - write_file
  - &try_pipeline
    name: try_pipeline
    description: >
      RUN a pipeline against the build under review and return its transcript.
      One argument, `yaml`: the full text of a small pipeline. It is built from
      the working tree, run with `steps test` (every job, in order, evaluating
      any `assert:`), and the output plus the exit code come back to you.
      This is how you CHECK a claim instead of inferring it. Use it on every
      behavioral bullet the diff adds to docs/, on every acceptance criterion
      in the story, and on any control-flow reasoning of your own you are about
      to cite -- write the smallest pipeline that would be WRONG if the claim
      held, and run it.
      TO CHECK A SHIPPED EXAMPLE, read_file repo/examples/<name>.yml and pass
      its TEXT here as `yaml`. Do NOT shell out to `steps validate` or
      `steps test` from inside a probe, and do not go looking for the binary:
      `steps` is not on PATH in this sandbox, so that returns a bare exit 127
      that looks like a defect in the change and is not one. This tool
      already IS the runner.
      TO CHECK THAT A BAD CONFIG IS REJECTED, make the BAD PIPELINE ITSELF
      the `yaml`. It fails at load, the loader's error text is in the
      transcript you get back, and PROBE EXIT is non-zero -- that IS the
      evidence, and it is the whole technique. Do not wrap it in a task and
      assert on stdout: a task's stdout is the task's, not the loader's, so
      that probe can only ever fail. Empty blocks, missing required keys,
      a field on the wrong step kind, two steps with one name -- all of them
      are one probe each, and the assertion is simply that the run refused. A non-zero exit is a normal, often expected result, not
      a tool failure. Keep each pipeline to a few steps; `run: exit 1` and
      `run: echo MARKER` are usually all a probe needs.
      MAKE THE PROBE SELF-CHECKING wherever you can, with a job-level
      `assert: {execution: [a, b, c]}` naming the exact steps and hooks that
      must run, or a step `assert: {stdout: MARKER}`. Then PROBE EXIT is the
      answer and you are not eyeballing a transcript: a mismatch prints
      `want: [...]` / `got: [...]`, which names the missing step for you. Fall
      back to reading the transcript only for claims no assert can express.
      KEEP PROBES MODELLESS. This tool runs with the host env stripped to a
      short allowlist (PATH, HOME, locale, TMPDIR, proxies) -- no API keys, by
      design -- so an `agent:` step in a probe dies with `environment variable
      "..." (api_key_env) is not set`. That is the sandbox, NOT a defect in the
      change. task:/put:/get:/when:/to:/hooks/assert: all work fine and cover
      every control-flow claim. For a claim that genuinely needs a model, do
      not probe it: say so in the critique, and check instead that an e2e test
      covers it against the fake provider seam (newFakeLLM in
      fakeprovider_test.go) -- that is how this repo tests agent behavior.
      Two refusals you may see. `REFUSED` means the probe tried to run steps on
      a pipeline from inside a step; rewrite it to express the behavior
      directly. `HARNESS VOID` means this build failed an internal control --
      it broke `assert:` evaluation itself, so no probe result in this review
      means anything. That is a serious defect in the change: report it and
      FAIL. Every probe is bounded at 120s, and `PROBE EXIT: 124` means it
      hung -- usually a finding about the change, not about the probe.
    max_output_bytes: 8000
    run: |
      cd repo || exit 1
      # Built OUTSIDE the git tree: a binary inside repo/ would show up as a
      # `??` line in repo_diff and read as an untracked file under review.
      bin="$(cd .. && pwd)/steps-review-bin"
      build=$(go build -o "$bin" . 2>&1) || {
        printf 'BUILD FAILED -- cannot probe this tree:\n%s\n' "$build"
        exit 1
      }
      probe="$(mktemp -d)"
      printf '%s\n' {{ .args.yaml | shellquote }} > "$probe/probe.yml"

      # NO RE-ENTRY. A probe runs steps; letting it run steps ON this pipeline
      # (or on itself) nests the whole experiment inside one review turn --
      # cloning, agent calls, spend, and no bound. `steps` is not on PATH here,
      # which makes it unlikely rather than impossible: repo/ holds the source
      # and $bin is one directory up.
      # `validate`/`runs`/`mcp` are in this list even though they are cheap and
      # non-recursive. Leaving them out did not permit them -- `steps` is not
      # on PATH, so the probe just died with a bare `exit status 127`, which
      # reads as a defect in the change under review rather than as a sandbox
      # boundary. Observed: a reviewer spent a turn on
      # `cd repo && ./steps validate examples/parallel.yml` and got 127. An
      # explicit refusal that says what to do instead is strictly better than
      # a command-not-found.
      # Naming the review binary AT ALL is refused, in any context, before the
      # adjacency check below. Observed bypass: a probe wrote
      # `STEPS=/abs/path/steps-review-bin` on one line and `$STEPS run p.yml`
      # on the next. The name and the subcommand are then never adjacent, so
      # the pattern below -- which requires them next to each other -- matched
      # nothing and a full re-entry sailed through. Variable indirection
      # defeats any adjacency rule, so do not try to out-pattern it: a probe
      # has no legitimate reason to mention this binary, and that flat fact is
      # the check.
      if grep -qn 'steps-review-bin' "$probe/probe.yml"; then
        printf 'REFUSED: this probe names the review binary. A probe is a\n'
        printf 'pipeline for the harness to RUN -- it never invokes the binary\n'
        printf 'itself, under any spelling, including via a variable. The yaml\n'
        printf 'you pass is already executed by that binary.\n'
        exit 1
      fi
      if grep -qnE '(^|[^[:alnum:]_-])(steps|steps-review-bin|go[[:space:]]+run)[[:space:]]+(run|test|watch|plan|validate|runs|mcp|\.)([[:space:]]|$)|pipeline-agent\.yml' "$probe/probe.yml"; then
        printf 'REFUSED: this probe invokes steps (or this pipeline) from inside\n'
        printf 'a step. A probe is a pipeline to RUN, not a pipeline that runs\n'
        printf 'pipelines -- the harness already runs YOUR yaml with `steps test`,\n'
        printf 'so anything you shell out to is redundant at best.\n'
        printf 'TO CHECK A SHIPPED EXAMPLE: read_file repo/examples/<name>.yml\n'
        printf 'and pass its TEXT as this tool`s yaml argument. That runs the\n'
        printf 'real example through the real binary, which is what you wanted.\n'
        printf 'For anything else, express the behavior directly with\n'
        printf 'task:/put:/when:/to:/hooks/assert:.\n'
        exit 1
      fi

      # A bound. The probe is built from the tree UNDER REVIEW, so a change
      # that introduces an unbounded to: loop or a hung command would otherwise
      # spin here until the whole 30m step timeout is gone and the review is
      # lost. Two minutes is generous for a few steps.
      limit=120
      to=""
      for t in timeout gtimeout; do
        command -v "$t" >/dev/null 2>&1 && { to="$t $limit"; break; }
      done
      [ -n "$to" ] || printf 'NOTE: no timeout(1) here; this probe is UNBOUNDED.\n\n'

      # CONTROL FIRST. The probe binary is the artifact under test, so a change
      # that breaks assert evaluation would make every probe report PASS and
      # this tool would cheerfully confirm whatever it was asked. This control
      # asserts an execution list that CANNOT hold; a healthy build fails it
      # (exit 2). If it passes, the harness is lying and every other probe in
      # this review is void. Verified: patching checkExecution to always return
      # nil flips this control from exit 2 to exit 0.
      printf 'jobs:\n- name: harness-control\n  assert:\n    execution: [__no_such_step__]\n  plan:\n  - task: real\n    run: "true"\n' > "$probe/control.yml"
      crc=0
      (cd "$probe" && $to "$bin" test control.yml) >/dev/null 2>&1 || crc=$?
      if [ "$crc" -eq 0 ]; then
        printf 'HARNESS VOID: the control probe -- an assert that cannot hold --\n'
        printf 'PASSED on this build. assert evaluation is broken by the change\n'
        printf 'under review, so no probe result means anything. That is itself\n'
        printf 'a defect: report it and FAIL the review.\n'
        exit 1
      fi

      # internal/shell.HostEnv strips this shell to a short allowlist that
      # deliberately excludes every api_key_env secret, so an agent-backed
      # probe cannot authenticate and dies looking like a bug in the change.
      # Say so up front rather than letting the model misattribute it. Advisory
      # only -- probing that a config with an agent: is REJECTED at load time
      # is legitimate and never reaches credentials.
      if grep -qE '^[[:space:]]*-?[[:space:]]*agent:' "$probe/probe.yml"; then
        printf 'NOTE: this probe declares an agent:. No API keys reach this\n'
        printf 'sandbox, so if it fails on api_key_env that is the harness,\n'
        printf 'not the change under review. Probe control flow modelless.\n\n'
      fi
      rc=0
      out=$(cd "$probe" && $to "$bin" test probe.yml 2>&1) || rc=$?
      printf '%s\n' "$out" | tail -n 200
      [ "$rc" -eq 124 ] && printf '\n(timed out after %ss -- the probe hung)\n' "$limit"
      # Always exit 0: the probe's own exit code is the OBSERVATION, reported
      # here as data. Failing the tool call would read as "the tool broke" and
      # invite a pointless retry of a probe that worked exactly as intended.
      printf '\nPROBE EXIT: %s\n' "$rc"

# THREE coder steps, not one, because one conversation could not hold the
# work. Measured on story 002: 42 dispatch sites across 19 files, 148 tool
# calls, and the coder hit `agent.compaction_stalled -- recent window alone
# exceeds the budget`, meaning there was nothing left to compact and the
# conversation was simply too big. The spec fixtures fixed WHAT done means;
# they do nothing about how much fits in one context.
#
# The split follows this repo's own dependency graph (see CLAUDE.md), which
# is acyclic and enforced by depguard, so the layers really are orderable:
#
#   coder_config   internal/config + steps.schema.json   -- the leaf: types,
#                  validation, the schema. Nothing here can make a fixture
#                  pass on its own, so it is gated on BUILD only.
#   coder          internal/pipeline, merkle, workspace  -- the runtime that
#                  actually executes the feature. This is the layer that
#                  turns the fixtures green, so it gets the full gate.
#   coder_docs     docs/ and any remaining examples      -- prose last, once
#                  the behavior it describes is real and verified.
#
# Each is a FRESH conversation with its own budget, so the context that blew
# up is now three smaller ones, and each hands the next a runner-computed
# "Files touched" note. The middle one keeps the name `coder` so the review
# loop routes failures to the layer where semantic defects almost always
# live -- and its prompt says plainly that it may edit any package, because
# the split is about focus, not permission.
#
# This is the one persona doing sustained surgical Go edits, so it gets its
# own model slot rather than sharing *fast.
# Defined runtime-layer-first only because YAML anchors must precede their
# aliases; the PLAN order below is config -> runtime -> docs.
#
# This comment used to describe Qwen3 Coder Flash and ended "1M context so
# the conversation never needs compacting". Both halves outlived their truth
# and the second one did real damage: the slot moved to deepseek-v4-pro
# (also 1M) but "never needs compacting" was never TRUE of either, because
# compaction is on by DEFAULT at 102,400 tokens and no agent here overrode
# it. A comment asserting the problem could not happen sat directly above
# the agent that hit it. That is the doc-contradicts-code class this
# pipeline exists to catch, in the pipeline's own source -- so the budget is
# now set explicitly on every agent above, where it is checkable, rather
# than assumed in prose.
- name: coder
  # deepseek-v4-pro, same as *fast. This slot was split off to run a cheaper
  # coding model (the two personas want different things: one reads and
  # judges, one writes for 150 turns), and it briefly pointed at
  # deepseek-v4-flash -- which then went unavailable upstream mid-experiment,
  # timing out on a 5-token request while -pro on the same gateway answered
  # in seconds. Every failure in that run landed here and none on the
  # planner, which is the same-gateway control. Both slots are -pro now by
  # decision, not by outage; the anchor stays split so pointing this at a
  # different model again is one line.
  source: &coding { model: opencode/deepseek-v4-pro }
  max_turns: 200
  compact_after_tokens: *ctx_1m
  system_file: "@builtin/builder"
  tools:
  - mcp: gopls
    tools: [go_symbol_references, go_diagnostics]
    max_output_bytes: 6000
  - read_file
  - list_dir
  - search_files
  - edit_file
  - write_file
  - run_shell
  - &verify_build
    name: verify_build
    description: >
      COMPILE-ONLY gate: go fmt && go mod tidy && golangci-lint run && go
      build. No arguments. It deliberately does NOT run go test, because the
      spec fixtures in examples/ are RED by design until the runtime layer
      lands, and a layer that cannot turn them green must not be judged by
      them -- you would be staring at a failure that is not yours and cannot
      be fixed from here. What this DOES catch is everything that is yours:
      a type error, a lint violation, an unused import, a broken build.
      Green here means "this layer is internally sound and the next layer can
      build on it", which is the only claim you are in a position to make.
    run: |
      cd repo || exit 1
      rc=0
      out=$(go fmt ./... 2>&1 && go mod tidy 2>&1 && golangci-lint run 2>&1 \
            && go build -v 2>&1) || rc=$?
      printf '%s\n' "$out" | tail -n 300
      if [ "$rc" -eq 0 ]; then
        printf '\nBUILD GATE: PASS (tests NOT run -- fixtures stay red until runtime)\n'
      else
        printf '\nBUILD GATE: FAIL (exit %s) -- fix, then call verify_build again\n' "$rc"
      fi
      exit "$rc"
  - &verify_gate
    name: verify_gate
    description: >
      Run the whole objective gate in one call and report PASS or FAIL: go
      fmt && go mod tidy && golangci-lint run && go test ./... && go build.
      No arguments. Call it after every edit -- a pass from before your last
      change is worthless. Trimmed to the last 300 lines, where the failures
      are. Do not type this chain into run_shell yourself; getting the &&
      order wrong is how a past run reported success on a broken tree.
      Note `go test ./...` here also RUNS every examples/*.yml through
      `steps test` (TestExamplesRun), so an example that only looks right
      fails this gate. An example that genuinely cannot run offline -- it
      needs a live model, a network service, or docker -- opts out with a
      `# steps-test: skip -- <reason>` line, and the reason is not optional.
    run: |
      cd repo || exit 1
      rc=0
      out=$(go fmt ./... 2>&1 && go mod tidy 2>&1 && golangci-lint run 2>&1 \
            && go test ./... 2>&1 && go build -v 2>&1) || rc=$?
      printf '%s\n' "$out" | tail -n 300
      if [ "$rc" -eq 0 ]; then
        printf '\nGATE: PASS\n'
      else
        printf '\nGATE: FAIL (exit %s) -- fix, then call verify_gate again\n' "$rc"
      fi
      exit "$rc"

# The config layer. Gated on BUILD only: nothing in internal/config can turn
# a spec fixture green by itself, so holding this step to the fixture gate
# would hold it to a failure it structurally cannot fix.
- name: coder_config
  source: *coding
  max_turns: 200
  compact_after_tokens: *ctx_1m
  system_file: "@builtin/builder"
  tools:
  - mcp: gopls
    tools: [go_symbol_references, go_diagnostics]
    max_output_bytes: 6000
  - read_file
  - list_dir
  - search_files
  - edit_file
  - write_file
  - run_shell
  - *verify_build

# The docs layer, last, once the behavior it describes is real AND the
# fixtures prove it. Full gate: by this point green is achievable, and prose
# that contradicts working code is the single most common defect this
# experiment has produced.
- name: coder_docs
  source: *coding
  max_turns: 200
  compact_after_tokens: *ctx_1m
  system_file: "@builtin/builder"
  tools:
  - mcp: gopls
    tools: [go_symbol_references, go_diagnostics]
    max_output_bytes: 6000
  - read_file
  - list_dir
  - search_files
  - edit_file
  - write_file
  - run_shell
  - *verify_gate

# Quality gate before the put.
#
# No search_files and no list_dir, deliberately. Those are the open-ended
# exploration tools, and open-ended exploration is exactly what sank an earlier
# run: with a 25KB diff already in hand the reviewer spent most of its turns
# re-deriving internal/pipeline from scratch, pushing its context past 80k
# tokens and its latency to ~40s/turn, and it timed out before reaching a
# verdict. Its evidence is the diff and the planner's plan; read_file verifies
# a specific claim it intends to cite, and gopls answers the one question text
# search genuinely cannot (every call site of a symbol, for the sibling check).
#
# try_pipeline is the exception to "no execution", and it is here because the
# read-only posture had a specific, measured cost. This reviewer returned PASS
# on story 001; a second review found ten confirmed defects, and every one was
# confirmed by building the binary and running a three-line pipeline. Four of
# them were docs asserting behavior the code never had -- a class a reader
# cannot catch at all, since the doc and the code are separately plausible and
# only disagree at runtime.
#
# It is NOT a relaxation of the context-budget rule above: a probe is a
# pipeline the reviewer writes to answer one question, not a search. It IS a
# relaxation of the no-shell posture -- a probe's `run:` is a shell, so this is
# shell by proxy, and nothing confines it. That does not lower the pipeline's
# floor: `coder` already holds an unrestricted run_shell against the same
# workspace, so arbitrary model-authored shell was already inside the trust
# boundary. What the reviewer still cannot do is EDIT the repo: it has no
# edit_file at all, so it cannot touch a line of the implementation it is
# judging. Its write_file is scoped by convention to three things -- its
# critique, the PR files, and (since the spec-fixture redesign) a failing
# fixture under repo/examples/ pinning a defect it found. That last one is a
# real widening and it is deliberate: a reviewer that can only describe a
# defect hands the coder a paragraph, while one that can commit the pipeline
# that reproduces it hands over a test. It still cannot make anything PASS --
# a fixture only ever adds a way to fail -- so the direction of the widening
# is toward more scrutiny, not less.
- name: reviewer
  source: *fast
  max_turns: 200
  compact_after_tokens: *ctx_1m
  system_file: "@builtin/reviewer"
  tools:
  - mcp: gopls
    tools: [go_symbol_references]
    max_output_bytes: 6000
  - read_file
  - write_file
  - name: repo_diff
    description: >
      Show what this attempt actually changed. No arguments. Returns `git
      status --short`, `git diff --stat HEAD`, then `git diff HEAD` -- HEAD,
      not `main...HEAD`, because the implementer does not commit, so the work
      under review is UNCOMMITTED. A `??` line is a NEW file that will not
      appear in the diff, so read it with read_file. An oversized diff spills
      to a file; read_file accepts the absolute path from the pointer.
    run: |
      cd repo || exit 1
      printf -- '--- git status --short (a "??" line is a NEW file) ---\n'
      git --no-pager status --short
      printf -- '\n--- git diff --stat HEAD ---\n'
      git --no-pager diff --stat HEAD
      printf -- '\n--- git diff HEAD (the uncommitted work under review) ---\n'
      git --no-pager diff HEAD
  - *try_pipeline

# The SECOND verdict, and a different failure mode rather than a rerun. The
# reviewer above returned PASS on story 001; a second review found ten
# confirmed defects, and they split about evenly between "this is wrong" (the
# lens above, anchored on the diff) and "this case was never handled" -- which
# the diff cannot show you, because a case nobody handled appears in no diff.
#
# So this one gets NO repo_diff, deliberately. That is the whole design: the
# reviewer above is instructed to trust the plan and read the diff, which is a
# sound context-budget decision and also guarantees it inherits the plan's
# blind spots. "What code path exists that this plan never mentions?" is
# structurally unaskable there. Denying the diff here means this agent cannot
# anchor on what was written and has to start from what was ASKED FOR.
#
# It gets the exploration tools the reviewer above is denied (search_files,
# list_dir) for the same reason -- its job IS the open-ended search, and it
# spends no context on a 25KB diff to make room for it. try_pipeline is shared
# with the reviewer above by anchor, since "nothing implements this" is a
# claim a probe settles faster than any amount of reading.
- name: completeness
  source: *fast
  max_turns: 200
  compact_after_tokens: *ctx_1m
  system_file: "@builtin/reviewer"
  tools:
  - mcp: gopls
    tools: [go_symbol_references]
    max_output_bytes: 6000
  - read_file
  - list_dir
  - search_files
  - write_file
  - *try_pipeline

jobs:
- name: self-build
  plan:
  - get: stories
    version: every
  - get: repo
  - agent: planner
    inputs: [stories, repo]
    context_paths: [repo/CLAUDE.md]
    attempts: 2       # whole-conversation restart on a transport failure
    timeout: 2h
    # Forces a shift-change report for the coder before this step may end.
    # The plan document is the PR artifact; the note is the operational
    # handoff, and it carries a computed list of every file this step actually
    # touched -- which is what stops the coder re-walking the same tree.
    handoff: { note: true }
    prompt: |
      Plan ONE implementation step for ONE story. You have NO shell.
      The only file you write is the plan document.

      1. read_file on stories/body.md — that IS the story, the text of a
         GitHub issue, and it is the contract you are planning against. Its
         "Acceptance criteria" checklist is the definition of done; your plan
         must say which change satisfies each one. stories/num is the story
         number the paths below use, and stories/title is its one-line
         summary. (repo/CLAUDE.md is already available via read_file — follow
         it.)
      2. Find where the feature must integrate. The feature the story ASKS
         FOR does not exist yet, so never search for its own name. Relevant:
         repo/internal/{config,pipeline,merkle,store,trigger,agent}. For
         anything you plan to CHANGE, run the sibling check -- that is what
         tells you whether this story touches one code path or several. Do
         not repeat a query you have already run.
      2a. ASK THE OTHER QUESTION FIRST if this story ADDS A VARIANT to a
         type existing code switches on -- a new StepKind, a new NodeKind,
         a new outcome class, a new verdict, a new resource backend. "Where
         is the symbol I am editing used" is the wrong query for that shape:
         the new variant has no call sites yet, so the sibling check returns
         NOTHING and the story looks like it touches one file. Nothing in the
         diff will ever point at the sites that matter; only the type will.
         There are TWO queries, and story 001 -- one new StepKind, ten
         shipped defects, six of them a missed site -- needed both. Run both.

         QUERY 1: WHERE DOES EVERY EXISTING VARIANT GET HANDLED?
         search_files for the existing variant names. BOTH SPELLINGS COUNT,
         because this repo uses both for the same dispatch:
           - tagged:   `case config.StepKindPut:`  (internal/merkle,
             internal/pipeline/hooks.go, internal/config/assert.go)
           - tagless:  `case step.Put != "":`      (internal/config/artifact.go,
             internal/workspace/workspace.go, internal/pipeline/route.go)
         The tagless form is the dangerous one: it is a plain boolean switch,
         so neither the compiler nor .golangci.yml's `exhaustive` linter can
         see a case is missing. It silently does nothing for your new variant
         and every gate passes. Search at least two existing variants, not
         one -- a site handling put but not get is one your single query
         missed. This query found 2 of story 001's 6 sites.

         QUERY 2 -- only if your new variant CONTAINS OR WRAPS another step
         (try:, and any future wrapper). Then it is not merely a new switch
         case: every site that reads a FIELD off a step now reads it off the
         WRAPPER, where that field is empty or nil, and it will not switch on
         anything at all. This is why query 1 alone is not enough -- it finds
         switches, and these are field accesses. search_files for reads of
         the fields your story interacts with (`.Verdicts`, `.Handoff`,
         `.To`, `.Outputs`) and for `step.` in the validate* functions of
         internal/config. Story 001's other 4 missed sites were all this
         shape -- validateSegment, validateHandoffSegment,
         validateHandoffNoteSegment, handoffFor -- and all four were fixed
         the same way, by unwrapping to the inner step first. If an
         `Unwrap()` (internal/config/step.go) already exists, its call sites
         ARE this list for the previous wrapper: read them and ask which
         apply to yours.
      3. Read the files you found. Page the big ones.
      4. Write the plan to repo/experiments/self-build/plans/{num}.md. File
         by file: what changes, what tests are needed, which docs/*.md and
         examples/*.yml need updating. State explicitly whether the change applies to more
         than one structurally similar code path -- list the sibling sites
         gopls returned, with file:line -- and if so require a single test
         covering all of them. Do not describe a file you have not read.
      4a. If 2a applied, the plan MUST carry a "## Dispatch sites" table --
         every site BOTH queries returned, one row each, and a row is not
         optional because you decided the site is irrelevant:

           | Site (file:line) | Found by | Shape | New case belongs? | Why |
           |---|---|---|---|---|
           | internal/workspace/workspace.go:646 | Q1 | tagless step.X != "" | YES | wraps a producer, so its outputs must still flow |
           | internal/config/handoff.go:176 | Q2 | reads step.Handoff | YES | wrapper carries none, so unwrap first |
           | internal/config/route.go:32 | Q1 | tagged StepKind | NO | keyed on outcome, not kind |

         A NO is a real answer and costs one line; an ABSENT ROW is the
         defect this table exists to prevent. Story 001 added one StepKind
         and shipped ten defects; six were a site that handled every other
         kind and silently did nothing for the new one, and the 644-line plan
         named not one of them. Every gate passed. The reviewer walks the
         diff against this table row by row, so a row you cannot justify is
         cheaper to write now than a FAIL later.
  - agent: spec
    inputs: [stories, repo]
    context_paths: [repo/CLAUDE.md]
    attempts: 2
    timeout: 2h
    handoff: { note: true }
    prompt: |
      Turn this story into FAILING pipeline fixtures. You write no Go. You
      run BEFORE the implementer, against a tree where the feature does not
      exist yet, and that is the point: a fixture that passes right now is
      testing nothing, and you must not hand one over.

      1. read_file on stories/num and stories/body.md — the story, a GitHub
         issue. List EVERY requirement: each box under "Acceptance criteria",
         and each one stated only in PROSE (anything phrased as MUST,
         "otherwise", "so that", or naming a failure to avoid). Then read the
         plan at repo/experiments/self-build/plans/{num}.md for the syntax the
         feature will actually have — your fixtures must use the spelling the
         plan commits to, or they test a feature nobody is building.
      2. A FIXTURE MUST BE ABLE TO GO GREEN. Test this before you write one:
         "once the feature is correctly built, does `steps test` on this file
         EXIT 0?" If the answer is no, it does not belong in examples/, and
         the single biggest source of no is a NEGATIVE requirement -- one
         that says some config must be REJECTED at load. That pipeline fails
         by design, forever, and TestExamplesRun runs every examples/*.yml,
         so committing one pins the suite red no matter how correct the
         implementation is. The implementer would then be handed a target it
         cannot reach and no way to tell that from its own bug.
         Negative requirements are still real requirements: record them in
         the manifest with the Go test that should cover them (a LoadConfig
         call asserting the error), which is where a must-be-rejected claim
         belongs. Same for anything needing a model, and anything about log
         formatting a step assert cannot observe.
         For each requirement that DOES survive that test, write ONE fixture
         to repo/examples/<feature>-<aspect>.yml. Small and self-verifying:
         a job-level `assert: {execution: [a, b, c]}` naming the exact steps
         and hooks that must run, or a step `assert: {stdout: MARKER}`. Name
         the file for what it pins, not for the story.
         WHY examples/ AND NOT A SCRATCH DIRECTORY: schema_test.go validates
         every examples/*.yml against steps.schema.json, and TestExamplesRun
         RUNS every one of them. So `go test ./...` -- which is the coder's
         verify_gate and the build-check gate -- executes your fixtures with
         no new wiring. The suite stays RED until the feature is real, which
         is exactly the signal you are here to create. Never write
         `# steps-test: skip` on a fixture; that marker is for examples that
         genuinely cannot run offline, and using it here would silence the
         only thing you produce.
      3. PROVE EACH ONE IS RED. try_pipeline with the fixture's own text, on
         this unmodified tree. Expect a non-zero PROBE EXIT -- usually a load
         error, since the syntax does not exist yet. If a fixture PASSES, it
         asserts something already true: rewrite it until it fails, or drop
         it and say why. Report the observed red for each fixture.
         Keep fixtures MODELLESS. No API keys reach the probe sandbox or the
         test suite, so a fixture containing an `agent:` can never pass. For a
         requirement that genuinely needs a model, write no fixture -- record
         it in the manifest as needing an e2e test against the fake provider
         (newFakeLLM in fakeprovider_test.go) and move on.
      4. Write the manifest to repo/experiments/self-build/specs/{num}.md: a
         table of requirement -> fixture file -> the red you observed, plus a
         section listing requirements you could NOT express as a fixture and
         why. That section is not an admission of failure; it is the exact
         list of things the reviewers still have to judge by reading, and it
         is how they know where reading is still required.
  - agent: coder_config
    inputs: [stories, repo]
    context_paths: [repo/CLAUDE.md]
    attempts: 2
    timeout: 2h
    handoff: { note: true }
    prompt: |
      LAYER 1 of 3: internal/config and steps.schema.json ONLY. Two later
      steps do the runtime and the docs, so stop when this layer is sound --
      finishing the whole story here is what blew the context budget the last
      time one agent tried.

      0. `run_shell` on `cd repo && git status --short` first. A dirty tree
         means a previous attempt of THIS step was cut off mid-flight by a
         transport fault; continue from those edits rather than redoing them.
      1. read_file stories/num, the plan at
         repo/experiments/self-build/plans/{num}.md, and the spec manifest at
         repo/experiments/self-build/specs/{num}.md. The manifest lists the
         fixtures in repo/examples/ that define done. READ THE FIXTURES: they
         are the exact syntax users will write, so they tell you precisely
         which fields, types and validation this layer must support. A field
         a fixture uses and your types lack is a compile error later, in
         someone else's step.
      2. Implement ONLY: the config types, their YAML decoding, every
         validation rule, and steps.schema.json. CLAUDE.md is binding, and
         two of its rules bite hardest here -- one file per domain in
         internal/config, and a new config.Step field MUST appear in
         steps.schema.json or schema_test.go fails by reflection.
         If the plan carries a "## Dispatch sites" table, the rows in
         internal/config are yours; leave the rest.
      3. verify_build after every edit. NOT verify_gate: `go test ./...`
         includes the fixtures, which are red until layer 2 exists, so a
         failing test suite right now is the DESIGN and not your bug.
         verify_build must be PASS before you finish.
      4. Do not commit. In your handoff note, tell layer 2 the exact type and
         field names you created -- that is the interface it builds against,
         and it cannot see this conversation.
  - agent: coder
    inputs: [stories, repo]
    context_paths: [repo/CLAUDE.md]
    # attempts: covers transport faults only. Deliberately still 2, after a
    # run where raising it to 6 was the wrong reflex: the 500s and connection
    # resets it was meant to absorb all came from this step's model being down
    # upstream, and no retry count survives that. Each attempt restarts the
    # WHOLE conversation -- there is no per-request retry in the transport
    # stack (internal/agent/provider.go's agentHTTPClient) -- so a high
    # attempts: mostly buys a bigger bill for the same failure. Raise it only
    # for faults a fresh conversation could actually clear.
    attempts: 2
    timeout: 2h
    # EXPLICIT 2h, on every agent step. Omitting timeout: does NOT mean "no
    # deadline" -- internal/agent/step.go's agentTimeout falls back to
    # agentStepTimeout, a 10 MINUTE default, because the OpenAI client sets no
    # request timeout of its own and relies entirely on ctx. So deleting the
    # 20m/30m/45m values here did not remove the bound, it TIGHTENED it to ten
    # minutes, and the next run died on `context deadline exceeded` in
    # coder_config -- which retry correctly refuses to retry, since a fresh
    # conversation would hit the same wall.
    #
    # 2h is chosen to be effectively unreachable while still stopping a hung
    # endpoint from blocking forever, which is what agentStepTimeout is
    # actually for. The real bounds on spend are max_turns: and max_visits:,
    # which stop work without discarding what is already finished -- a
    # timeout ends the step outright (docs/attempts-timeout.md) and throws
    # away every turn already paid for. The probe sandbox keeps its own
    # separate 120s cap for a hung command inside the tree under review.
    # Both directions on one step: a redo gets a previous_run tool over the
    # last verdict (backward), and it writes the next agent a shift-change
    # report (forward).
    handoff: { tool: true, note: true }
    prompt: |
      Implement ONE story's already-written plan. repo/ is a real git clone,
      yours to edit.

      0. `run_shell` on `cd repo && git status --short && git diff --stat`
         BEFORE anything else. An `attempts:` restart throws away the
         conversation but NOT the workspace, so a dirty tree here means a
         previous attempt of THIS step was cut off mid-flight by a transport
         fault and its edits are still on disk. Read those files and continue
         from them -- do not redo work that is already there, and do not
         revert it. A clean tree means you are starting cold.
      1. read_file on stories/num, then on the plan at
         repo/experiments/self-build/plans/{num}.md. stories/body.md is the
         story itself (a GitHub issue) — read it too: the plan is an
         interpretation of it, and the issue's "Acceptance criteria" is what
         the reviewer will hold the result to. (repo/CLAUDE.md is already
         available via read_file — follow it exactly.)
         The planner's handoff note is ALREADY in your context: its
         "Files touched" section is computed by the runner, not written by
         the planner, so it is a reliable record of where the planner
         actually looked. Check it before you search for anything.
      1a. YOUR TARGET IS ALREADY WRITTEN AND ALREADY FAILING. The spec agent
         turned this story into fixtures under repo/examples/, and the
         manifest at repo/experiments/self-build/specs/{num}.md maps each
         requirement to the fixture that pins it and the red it observed.
         Read the manifest first. `go test ./...` runs every examples/*.yml
         (TestExamplesRun), so verify_gate is RED right now BY DESIGN, and it
         goes green exactly when the feature is real. That is your definition
         of done, and it is worth more than any prose: you can run it.
         The manifest's "could not express as a fixture" section is the part
         no test covers. Implement those with the same care and expect to be
         judged on them by reading.
         NEVER make a fixture pass by weakening it. Do not delete one, do not
         loosen its assert:, do not add `# steps-test: skip`, do not rename it
         out of examples/. A fixture you believe is genuinely WRONG -- it
         asserts something the story never asked for -- is a legitimate
         finding, but it is a thing you REPORT in your handoff note, leaving
         the fixture untouched, not a thing you fix by deletion. The review
         checks the fixtures against the manifest, so a quiet edit reads as
         exactly what it is.
      1b. YOU ARE LAYER 2 OF 3. Layer 1 already did internal/config and the
         schema -- its handoff note, above, names the exact types and fields
         it created, and that is your interface. Layer 3 does docs/ after
         you. So your work is the RUNTIME: internal/pipeline, internal/merkle,
         internal/workspace, internal/agent, and the Go tests for them.
         You MAY edit any package -- if layer 1 got a type wrong, fix it,
         that is not a boundary violation. The split is about where your
         attention goes, not about permission. But do not write docs/ prose;
         layer 3 does that once your behavior is real and the fixtures prove
         it, and prose written before the behavior is how this experiment has
         repeatedly shipped a doc that contradicts its own code.
         You are the layer that turns the fixtures GREEN. That is your
         definition of done and you can run it.
      2. Implement it end to end: runtime code and tests, following CLAUDE.md
         exactly. If the story adds a behavior or guard to one of several
         similar code paths, run the sibling check and apply the SAME
         treatment to all of them, not just the first.
      3. After each edit, gopls__go_diagnostics on the file you touched.
         gopls has no LSP client feeding it your live edits, so its view can
         go stale: verify_gate is the real gate, not diagnostics.
      4. verify_gate. Keep fixing and re-calling until it says PASS, and call
         it again after EVERY further edit -- a PASS from before your last
         change proves nothing.
      5. Do not commit. A later step publishes only if the review passes.

      If a `<transition_context>` block appears below, you were sent back
      here after a previous attempt. A failed build/lint/test gate: just call
      verify_gate, your changes are still here so you will see the errors
      directly. A failed review: call previous_run for the reviewer's note,
      then read BOTH critiques, since either reviewer can send you back and
      they fail for different reasons --
      repo/experiments/self-build/critiques/{num}.md is "this is wrong" and
      {num}-completeness.md is "this was never implemented". The second one
      may not exist if the first reviewer failed you; that is normal.
  - agent: coder_docs
    inputs: [stories, repo]
    context_paths: [repo/CLAUDE.md]
    attempts: 2
    timeout: 2h
    handoff: { note: true }
    prompt: |
      LAYER 3 of 3: documentation. The feature is implemented and its
      fixtures are green. You write prose, and you write it LAST on purpose:
      every doc-versus-code contradiction this experiment has produced came
      from describing behavior before it existed. Story 001 shipped four of
      them; story 002's headline docs example used syntax the code rejects at
      load time, and only the final reviewer caught it.

      1. read_file stories/num and stories/body.md (the story), the spec
         manifest at repo/experiments/self-build/specs/{num}.md, and the two
         handoff notes above -- between them they say what actually got
         built, which is not always what the plan predicted.
      2. Update docs/*.md per the story. The relevant files are named in the
         plan; docs/README.md indexes them.
         EVERY YAML SNIPPET YOU WRITE MUST BE ONE THAT RUNS. The fixtures in
         repo/examples/ are known-good, verified syntax -- prefer adapting
         one over inventing a snippet from the story's prose, which was
         written before the feature existed and may describe a shape that was
         never built. If you write a snippet that is not lifted from a
         passing fixture, verify it: `run_shell` on
         `cd repo && go build -o /tmp/s . && /tmp/s validate <file>`.
      3. State the limits, not just the happy path: what the feature REFUSES
         (every validation layer 1 added is a sentence users need), and any
         interaction with existing features that surprised the runtime layer.
      4. verify_gate -- the full one, and it must PASS. Green is achievable
         now, so a red suite here is a real regression you introduced.
      5. Do not commit.
  - task: build-check
    inputs: [repo]
    run: |
      set -o pipefail
      cd repo

      # FIXTURE TAMPERING, checked before anything else. The spec agent wrote
      # failing fixtures into examples/ and the whole design rests on the
      # coder making them pass. There is one cheap way to make a failing test
      # pass that is not implementing the feature -- silence it -- so the two
      # spellings of silence are refused mechanically here rather than left
      # to a reviewer to notice.
      #
      # `# steps-test: skip` is the real risk: it is a documented, legitimate
      # marker (for examples needing a live model, network or docker), which
      # is exactly what makes it a plausible-looking way out. Any NEWLY added
      # skip marker is refused; pre-existing ones are untouched, so the
      # examples that genuinely need it keep working.
      if git diff -U0 -- examples/ | grep -qE '^\+.*steps-test:[[:space:]]*skip'; then
        echo "GATE: a 'steps-test: skip' marker was ADDED to examples/."
        echo "That marker exempts an example from TestExamplesRun. Adding one"
        echo "during implementation silences a fixture instead of satisfying"
        echo "it. If a fixture is genuinely wrong, say so in the handoff note"
        echo "and leave it failing -- do not skip it."
        git diff -U0 -- examples/ | grep -E '^\+.*steps-test:[[:space:]]*skip'
        exit 1
      fi
      if git diff --diff-filter=D --name-only -- examples/ | grep -q .; then
        echo "GATE: an examples/ fixture was DELETED."
        echo "Deleting the test is not passing the test. Restore it; report a"
        echo "fixture you believe is wrong instead of removing it."
        git diff --diff-filter=D --name-only -- examples/
        exit 1
      fi

      # Objective gate: cheap and deterministic, so an obviously-broken
      # attempt is rejected before spending reviewer turns on it. With the
      # spec agent upstream this is also the FIXTURE gate -- go test runs
      # every examples/*.yml, so "the story is implemented" and "this command
      # exits 0" became the same statement.
      #
      # `go test ./...` now also RUNS every example that isn't explicitly
      # marked `# steps-test: skip` (TestExamplesRun), so a shipped example
      # that doesn't execute fails here rather than reaching the reviewer.
      golangci-lint run && go test ./... && go build -v || exit 1

      # Structural coverage. The gate above can only run tests that exist, so
      # it cannot fail an attempt that adds user-visible surface and covers it
      # with nothing. steps.schema.json is the tightest available proxy for
      # "user-visible surface": CLAUDE.md requires a new config.Step field to
      # appear there, and schema_test.go enforces that by reflection -- so a
      # touched schema means new pipeline syntax, and new pipeline syntax with
      # no example is undemonstrated and unexercised. A pure refactor or an
      # internal bug fix never touches it, which is what keeps this from
      # firing on work that legitimately ships no example.
      changed="$(git status --porcelain | awk '{print $NF}')"
      if printf '%s\n' "$changed" | grep -q '^steps\.schema\.json$'; then
        if ! printf '%s\n' "$changed" | grep -q '^examples/'; then
          echo "GATE: steps.schema.json changed but no examples/ file did."
          echo "New pipeline syntax needs a runnable example -- ideally a"
          echo "self-verifying job with assert.execution, which is what makes"
          echo "it a regression fixture instead of a snippet. Add one, or"
          echo "explain in the plan why this syntax cannot be demonstrated."
          exit 1
        fi
      fi
    to:
      failure: coder
    max_visits: 10
  - agent: reviewer
    inputs: [stories, repo]
    context_paths: [repo/CLAUDE.md]
    attempts: 2
    timeout: 2h
    prompt: |
      You are the quality gate before this story's changes are published.
      Lint, tests and build already passed — your job is the correctness a
      compiler cannot check. You do not edit files. A concern you cannot
      cite as file:line is not a concern.

      The diff and the plan are your evidence. You are not exploring this
      codebase -- the planner already did, and its findings are in the plan.
      Read a file only to check a specific claim you intend to cite.

      Reading is the weaker of your two instruments. try_pipeline RUNS a
      pipeline against this build and hands you the transcript, and a claim
      you have RUN is worth more than a claim you have reasoned about. The
      last time this step passed a change on reading alone, it approved ten
      defects, and every one of them was a three-line pipeline away from
      being obvious. Prefer the probe. Budget for it: a probe costs seconds,
      and you have 200 turns.

      The coder's handoff note is already in your context. Everything the
      coder WROTE in it is a claim, not evidence -- verify each against the
      diff before relying on it, and treat its "watch_out" section as a list
      of places to look hardest, not as reassurance. Its final "Files
      touched" section is different: the runner computed that from the run
      itself, so it is mechanical truth, and it tells you which parts of the
      diff deserve your attention first.

      1. read_file on stories/num and stories/body.md — the story itself, a
         GitHub issue. Its "Acceptance criteria" checklist is the contract:
         your critique must walk it item by item and cite, per item, the
         file:line or the probe that satisfies it. An item you cannot cite is
         a FAIL. The checklist is not the whole contract, though: a
         requirement stated only in the issue's PROSE binds exactly as hard
         and is the kind the plan cannot warn you about, because it has no
         box to leave unticked. Anything phrased as MUST, "otherwise", or
         naming a failure to avoid goes on your list too. Then
         read the plan the implementer worked from,
         repo/experiments/self-build/plans/{num}.md -- it names the files that
         were supposed to change and the sibling sites gopls returned.
         (repo/CLAUDE.md is already available via read_file.)
      2. repo_diff. A `??` line is a NEW file that does not appear in the
         diff at all -- read each of those with read_file.
      3. PROBE, before you judge. Build a list of every behavioral claim this
         change makes -- each bullet the diff adds or edits under docs/, each
         acceptance criterion in the story, each new example job -- and run
         try_pipeline on each one. Write the pipeline that would be WRONG if
         the claim held, and give it an `assert:` so the probe answers itself.
         Three that would have caught real defects:
           - "the inner step's hooks still fire" -> wrap a failing task whose
             on_failure runs `alert`, and assert
             `execution: [flaky, alert, after]`. On a build where the hook is
             skipped this FAILS with `got: [flaky after]` -- it names the
             missing hook for you.
           - "routing sees the real outcome" -> give the step a
             to: {failure: X, success: Y} and assert the execution list
             contains X, not Y.
           - "steps test still catches a broken assert" -> write the broken
             assert and see whether the run reports PASS.
         A claim whose probe disagrees with the doc or the story is a FAIL --
         it does not matter which of the two you think is right.
         Probes are MODELLESS: no API keys reach the probe sandbox, so an
         `agent:` step in one fails on api_key_env no matter how correct the
         change is. Control flow, artifacts, caching, validation and every
         assert are all reachable without a model. For a claim that truly
         needs one, do not probe and do not guess -- say so in the critique,
         and check instead that an e2e test exercises it against the fake
         provider (newFakeLLM in fakeprovider_test.go). A story whose central
         behavior is agent-side and ships with no such test is a FAIL on 4a.
      4. Judge whether the story is fully AND CORRECTLY implemented, not
         merely plausible-looking:
         a. the feature described is present, with tests that exercise it;
         b. docs/examples are updated if the story called for them, and every
            claim they make about behavior is true of the code next to it --
            a comment contradicting the code is a fail, not a nitpick, and
            this is what step 3 is for;
         c. if the change touches one of several similar code paths, run the
            sibling check and confirm the SAME treatment landed on all of
            them -- a fix applied to only one is a fail;
         c2. if the plan carries a "## Dispatch sites" table, WALK IT ROW BY
            ROW against the diff. This is mechanical and it is the one check
            you can make without re-exploring: a YES row whose file:line does
            not appear in the diff is a FAIL, and say which row. If the story
            adds a variant to a type existing code switches on (a StepKind, a
            NodeKind, an outcome class) and the plan has NO such table, that
            is itself a FAIL -- the plan skipped the analysis, so you cannot
            inherit it. Do not re-derive the table yourself; you do not have
            the budget, and saying the plan is missing it is the finding.
            Spot-check ONE row you doubt with search_files for an existing
            variant name in both spellings (`case config.StepKindPut:` and
            `case step.Put != "":` are the same dispatch, and the tagless
            one is invisible to the compiler and to `exhaustive` alike).
            If the new variant WRAPS a step, weight the table's Q2 rows
            hardest: those sites read a field off a step, so on the wrapper
            they read nil and do nothing quietly -- four of story 001's six
            misses were exactly that, and none of them is a switch anything
            could have flagged. A handled-nowhere variant usually probes
            cleanly too: build the smallest pipeline using the new variant
            where the missing site would matter, and assert the behavior
            that site is supposed to provide;
         d. edge cases the story implies (empty, zero, negative, malformed)
            are handled deliberately, not silently accepted;
         e. every example the change ships under examples/ actually RUNS --
            probe it. A non-runnable example passes lint, tests and build
            without complaint, because nothing executes it.
       4f. CHECK THE FIXTURES AGAINST THE MANIFEST. Read
          repo/experiments/self-build/specs/{num}.md and confirm every fixture
          it lists still exists in repo/examples/, still asserts what the
          manifest says it asserts, and carries no `# steps-test: skip`. A
          fixture that was weakened, renamed away, or skipped is a FAIL on its
          own, whatever the rest of the diff looks like -- the gate went green
          by silencing the test rather than satisfying it. The manifest's
          "could not express as a fixture" section is the list of claims no
          test covers: those are where your own probes and reading are still
          the only instrument, so spend your effort there rather than
          re-checking what the suite already runs.
       5. Write your critique to repo/experiments/self-build/critiques/{num}.md
          — first line exactly PASS or FAIL, then your reasoning with file:line
          citations. Cite a probe by its pipeline and the line of transcript
          that settled it, the same way you would cite a file:line. That file
          must exist before you call the verdict tool.
       5a. A DEFECT YOU FOUND BY PROBE BECOMES A FIXTURE. If a probe of yours
          failed on this build, you have already written the pipeline that
          proves it -- write that same pipeline to
          repo/examples/<what-it-pins>.yml with its assert:, and name the file
          in your critique. Do this BEFORE the verdict, and do it whichever
          way you are voting: on a FAIL it hands the coder a runnable repro
          instead of a paragraph, and on a PASS it means the defect you nearly
          shipped can never come back silently. This costs you one write_file
          and it is the difference between a finding and a regression test.
          Do not add `# steps-test: skip` to it.
       6. If you intend to choose "pass" (the change passes review), write
          these files with write_file BEFORE calling the verdict tool — they
          become the PR:
          a. repo/experiments/self-build/prs/{num}/title — a one-line PR
             title like "story 001: short description of the change"
          b. repo/experiments/self-build/prs/{num}/body.md — the PR
             description: a concise summary of the story, what changed
             (files and modules touched), and that lint/test/build/review
             passed.
          For a "fail" verdict skip these files entirely.
       7. Call the verdict tool with exactly "pass" or "fail" and a short
          `note` — THIS routes the pipeline and is what the implementer sees
          on a redo.
    verdicts: [pass, fail]
    to:
      # A pass here is no longer a publish -- it is a promotion to the second
      # reviewer. BOTH must pass to reach the put; either one FAILs back to
      # the coder, which is what makes this a gate rather than a second
      # opinion nobody is bound by.
      pass: completeness
      fail: coder
      failure: coder   # reviewer errored or never called verdict
    # Sized for TWO reviewers. With one gate, a story needing N coder passes
    # cost N visits here; with a second gate that can send the coder back
    # AFTER this one passed, the same story can revisit this step several
    # more times without anything being wrong. Story 002 burned all 3 of the
    # old budget before completeness had reviewed even once -- exhausting
    # max_visits kills the run outright, discarding an hour of finished work
    # over a bound that was only ever meant to stop a runaway loop. 10 still
    # stops one; it just does not stop ordinary progress.
    max_visits: 10
  - agent: completeness
    inputs: [stories, repo]
    context_paths: [repo/CLAUDE.md]
    attempts: 2
    timeout: 2h
    prompt: |
      You are the SECOND of two reviewers and you are looking for what is
      MISSING. The first reviewer already asked "is what was built correct?"
      and answered it against the diff; it passed, or you would not be
      running. Asking that question again buys nothing. Yours is the one the
      diff cannot answer: WHAT DOES THIS STORY REQUIRE THAT NOTHING
      IMPLEMENTS?

      You have NO repo_diff, on purpose. A case nobody handled appears in no
      diff, and a requirement nobody implemented appears in no diff either --
      so the diff is not merely unhelpful for your question, it actively
      anchors you on what was written instead of what was asked. Start from
      the story. The tree is the evidence: search_files and list_dir are
      yours, and unlike the first reviewer you are EXPECTED to explore.

      The plan is a suspect, not a source. The first reviewer is instructed
      to trust it; that is why you exist. When the plan omitted a code path,
      the implementer never touched it and the first reviewer never looked
      for it -- so the omission survives every gate up to you. Read the plan
      to learn WHAT WAS NOT CONSIDERED, never to be reassured.

      1. read_file on stories/num and stories/body.md — the story, a GitHub
         issue. Then extract EVERY REQUIREMENT it states, and do this before
         you look at any code, so the list is the story's and not the
         implementation's. Two sources, both binding:
           - each box in "Acceptance criteria";
           - each requirement stated only in PROSE. These are the ones that
             get lost. Story 001 said in a plain sentence that `steps test`'s
             `assert:` must check the inner step's real outcome, not the
             swallowed one -- it shipped broken and the critique never
             mentioned it, because prose has no checkbox to leave unticked.
         Anything phrased as MUST, "otherwise", "so that", or naming a
         failure to avoid is a requirement. Write the list down first.
      2. For each requirement, find the thing that satisfies it and cite it
         as file:line, or a probe. search_files from the requirement's own
         words. A requirement you cannot cite is a FAIL, and "the plan says
         it was done" is not a citation.
      3. PROBE the requirements that describe BEHAVIOR. try_pipeline runs a
         pipeline against this build; a claim you have run outranks a claim
         you have read, and "nothing implements this" is exactly the claim a
         probe settles fastest -- write the pipeline that only passes if the
         requirement holds, give it an `assert:`, and let PROBE EXIT answer.
         Probes are MODELLESS (no API keys reach that sandbox), so for a
         requirement that genuinely needs a model, check instead that an e2e
         test covers it against the fake provider (newFakeLLM in
         fakeprovider_test.go) and say so.
      4. Then ask what the story implies that it never spells out:
         a. a code path that handles every existing case but not the new one
            -- if the plan carries a "## Dispatch sites" table, the rows it
            does NOT contain are your search space, not the rows it does;
         b. a doc or example that now describes behavior nobody verified;
         c. an edge case (empty, zero, negative, malformed) the story implies
            and nothing rejects deliberately.
      4d. Read the spec manifest at
         repo/experiments/self-build/specs/{num}.md. Two different things
         there matter to you, in opposite directions. Its fixture table is
         requirements ALREADY covered by a test the suite runs -- do not
         re-probe those, the gate did it. Its "could not express as a
         fixture" section is the opposite: requirements nothing mechanical
         checks, which makes them precisely your territory. Start there.
         Then ask the question the manifest itself cannot: which requirement
         from YOUR step-1 list appears nowhere in this manifest at all? The
         spec agent read the same story you did and may have missed the same
         prose you nearly missed. A requirement absent from both the manifest
         and the code is the deepest miss available here, and you are the
         last thing standing between it and a merge.
      5. Write repo/experiments/self-build/critiques/{num}-completeness.md —
         first line exactly PASS or FAIL, then the requirement list from step
         1 with its citation or probe per line. A requirement with no
         citation is what a FAIL looks like; name it in the note.
      5a. A requirement you found uncovered, that a pipeline COULD express,
         becomes a fixture: write it to repo/examples/<what-it-pins>.yml with
         its assert:, and name it in the critique. You are the last reviewer,
         so a gap you only describe is a gap that ships; a gap you commit as
         a failing fixture cannot. Never mark it `# steps-test: skip`.
      6. Call the verdict tool with exactly "pass" or "fail" and a short
         `note`. A pass here publishes the change, so pass only when every
         requirement on your list has a citation.
    verdicts: [pass, fail]
    to:
      pass: repo
      fail: coder
      failure: coder   # reviewer errored or never called verdict
    # Sized for TWO reviewers. With one gate, a story needing N coder passes
    # cost N visits here; with a second gate that can send the coder back
    # AFTER this one passed, the same story can revisit this step several
    # more times without anything being wrong. Story 002 burned all 3 of the
    # old budget before completeness had reviewed even once -- exhausting
    # max_visits kills the run outright, discarding an hour of finished work
    # over a bound that was only ever meant to stop a runaway loop. 10 still
    # stops one; it just does not stop ordinary progress.
    max_visits: 10
  - put: repo
    params: { path: repo }
